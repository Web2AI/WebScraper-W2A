# Web2AI Scraper


## CI checks

To run checks locally run: (`python -m`)

`black --check .`

`isort --check-only .`

`mypy .`

`bandit -r .` 

## Formatting

To format locally run: (`python -m`)

`black .`

`isort .`